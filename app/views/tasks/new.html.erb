<%= form_with model: @task, class: 'create-task' do |f| %>

<p>Please provide the following information to save the task to your list:</p>

    <%= f.label :name %>
    <%= f.text_field :name %>
    <br>
    <%= f.label :description %>
    <%= f.text_area :description %>
    <br>
    <br>
    <%= f.submit "Save Task", class: "task-button" %>
<% end %> 