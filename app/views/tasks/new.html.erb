<%= form_with model: @tasks, class: 'new-book' do |f| %>
  <h2>Need to do more stuff? Submit a new task!</h2>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit "Save Task", class: "task-button" %>
<% end %>


