<%= form_with model: @task do |f| %>
  <p>Add a task with description:</p>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>


<% end %>