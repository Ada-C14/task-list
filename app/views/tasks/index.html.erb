<h1>All Tasks</h1>


<% if @tasks.length > 0 %>

    <% @tasks.each do |task| %>
    <h2><%= task.name %></h2>
    <ul>
      <li><%= task.description %></li>
      <li>Completed on: <%= task.completed_at %></li>
    </ul>
    <% end %>

<% else %>
  <p>No tasks in the system!</p>
<% end %>
