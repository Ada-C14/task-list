<h1>Tasks list</h1>
<% if @tasks.length > 0 %>
    <ul>
    <% @tasks.each do |task| %>
        <li> <%= link_to task.name, task_path(task) %></li>
        <%end %>
    </ul>

<% else %>
        <h3>No tasks</h3>
<% end %>