<h2>Tasks List:</h2>
<ul>
  <% if @tasks.length > 0 %>
    <% @tasks.each do |task| %>
      <li><%= task.name %>: <%= task.description %>, completed at: <%= task.completed_at %></li>
    <% end %>
  <% else %>
    <p> No tasks in list</p>
  <% end %>
</ul>

