<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task|  %>
    <li>
      <%= link_to task.name, tasks_path(task) %>
      <ul>

        Description: <%= task.description %> <%= link_to "Edit Task", edit_task_path(task) %>
      </ul>
    </li>

  <% end %>
</ul>
<%= link_to "Add Task", new_task_path %>