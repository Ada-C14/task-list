<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to "#{task.name}: #{task.description}", task_path(task.id) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a New Task", new_task_path %>

