<h2>Task List</h2>

<%= "Check this everyday!".upcase %>

<% if @tasks.count > 0 %>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= task[:to_do_list] %> due by <%= task[:due_date] %></li>
    <% end %>
  </ul>
<% else %>
  <h3>No tasks in the list!</h3>
<% end %>