<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task|  %>
    <li>
      Task: <%= link_to task.name, task_path(task.id) %> Description: <%= task.description %> Date completed: <%=display_task_completed_at(task)%>
    </li>
  <% end %>
</ul>