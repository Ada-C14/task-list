<h1>Tasks</h1>
<h2>To-do:</h2>
<ul>
  <% Task.all.each do |task| %>
    <li>
      <%= task.name %>: <%= task.description %> ||
        status: <%= task.completed_at.nil? ? "Incomplete" : task.completed_at %>
    </li>
  <% end %>
</ul>