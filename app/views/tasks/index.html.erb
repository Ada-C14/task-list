<h1>TASKS LIST</h1>
<ul>
  <%@tasks.each do |task| %>
    <li>
      <%= link_to(task.name, task_path(task.id)) %>,
      Description: <%= task.description %> ,
      completed by <%= task.completed_at %>
    </li>
  <% end %>
</ul>
<%= link_to "Add Task", new_task_path %>
