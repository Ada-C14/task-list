<h1>Tasks</h1>
<% if @tasks.length > 0 %>
    <ul>
        <% @tasks.each do |task|  %>
            <li><%= task %></li>
        <% end %>
    </ul>
<% else %>
    <p>No tasks in the system</p>
<% end %>