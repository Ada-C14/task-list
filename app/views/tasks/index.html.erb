<h2>Your Tasks</h2>

<% if @tasks.length > 0 %>
  <ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.name, task_path(task) %>
    </li>
  <% end %>
  </ul>
<% else %>
  <p>You have no tasks.</p>
<% end %>