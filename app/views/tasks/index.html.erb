<h1>TASKS LIST</h1>
<h2>Baseline</h2>
<%@tasks.each do |task| %>
  <li> <%= link_to(task.name, task_path(task.id)) %>, Description: <%= task.description %> , completed by <%= task.completed_at %></li>
<% end %>