<h1>Tasks</h1>
<% if @tasks.length > 0 %>

<ul>
  <% @tasks.each do |task|  %>
    <li>
      Task Name: <%= task.name %> | About: <%= task.description %> | Completed On: <%= task.completed_at %>
    </li>
  <% end %>
</ul>

<% else %>
  <h3>No tasks in the system!</h3>
<% end %>
