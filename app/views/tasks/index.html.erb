<h1>Welcome!</h1>
<h2>Let's checkout our tasks:</h2>
<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to task.name, task_path(task)%> || <%=task.description %> || <%= task.completed_at %>
  </li>
    <%end %>
</ul>