<%= form_with model: @task do |f| %>
  <p>Please provide the following information:</p>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit "#{action_name} Task" %>
<% end %>